# Kylin_BaseLine&yumConfig

## 项目概述

本项目是针对麒麟操作系统的基线加固和yum配置管理工具集，旨在研究***一键安服***解决方案，用于部署和配置麒麟服务器(Kylin Linux Advanced Server V10 SP3)的安全基线和yum源设置。

## 标准规范参考依据

**1. GB/T 22239-2019 《信息安全技术 网络安全等级保护基本要求》（三级）**
**2. KYD/T 2701-2014 《电信网和互联网安全防护基线配置要求及检测要求》**
**3. KYJS-KS-Server-6-SHM-V1.0 《麒麟系统安全加固指南》**

## 目录结构

```
├── batch_deploy.sh           # 批量部署脚本
├── config_kylin_yum.sh       # 麒麟系统yum配置脚本
├── generate_server_list.sh   # 生成服务器列表脚本
├── generate_xshell_detailed_csv.sh  # 生成XShell导入CSV文件脚本（资产表一键导入xshell）
导入├── kylin_baseline.sh         # 麒麟基线加固脚本
├── setup_ssh_免密.sh         # SSH免密设置脚本
├── source/                   # 资产表源文件目录
│   ├── 资产表-互联网.csv      # 互联网资产表
│   ├── 资产表-政务外网.csv    # 政务外网资产表
│   └── 资产表.csv            # 综合资产表
├── 互联网结果/                # 互联网服务器扫描结果目录
│   ├── *.md                  # 各服务器基线检查结果Markdown文件
│   ├── Results_csv/          # CSV格式结果文件
│   ├── beautify_markdown.py  # Markdown美化脚本
│   ├── check_header.py       # 头部检查脚本
│   └── format_markdown.py    # Markdown格式化脚本
└── 政务外网结果/              # 政务外网服务器扫描结果目录
    ├── *.md                  # 各服务器基线检查结果Markdown文件
    ├── Results_csv/          # CSV格式结果文件
    └── beautify_markdown.py  # Markdown美化脚本
```

## 快速导航

### 主要脚本文件

- [batch_deploy.sh](batch_deploy.sh) - 批量部署脚本，用于批量执行基线加固操作
- [config_kylin_yum.sh](config_kylin_yum.sh) - 配置麒麟系统的yum源脚本
- [kylin_baseline.sh](kylin_baseline.sh) - 麒麟系统基线加固的核心脚本
- [setup_ssh_免密.sh](setup_ssh_免密.sh) - 配置SSH免密登录脚本

### 生成工具脚本

- [generate_server_list.sh](generate_server_list.sh) - 从资产表生成服务器列表
- [generate_xshell_detailed_csv.sh](generate_xshell_detailed_csv.sh) - 生成XShell连接配置CSV文件

### 结果目录

- [互联网结果/](互联网结果/) - 互联网环境服务器的基线检查和加固结果
- [政务外网结果/](政务外网结果/) - 政务外网环境服务器的基线检查和加固结果

### 源文件目录

- [source/](source/) - 包含各种环境的资产表CSV文件

## 使用说明

### 1. yum源配置

1. **上传脚本**：将以下脚本上传到主节点服务器的`/home/yumConfig/`目录下
   - `config_kylin_yum.sh`
   - `setup_ssh_免密.sh`
   - `generate_server_list.sh`

2. **配置SSH免密（可选）**：
   ```bash
   ./setup_ssh_免密.sh
   ```

3. **执行yum源配置**：
   ```bash
   ./config_kylin_yum.sh
   ```
   - 运行时需要传入源IP
   - 可在`.yumIP.env`文件中查找并回填源IP
   - 如未提前配置SSH免密，脚本会自动调用`setup_ssh_免密.sh`完成配置

### 2. BaseLine基线加固配置

1. **上传脚本**：将以下脚本上传到主节点服务器的`/home/baseline/`目录下
   - `batch_deploy.sh`
   - `generate_server_list.sh`
   - `kylin_baseline.sh`

2. **生成服务器列表**：
   ```bash
   ./generate_server_list.sh
   ```
   - 会生成`servers.txt`和隐藏密码文件`.server_passwords.txt`

3. **配置SSH免密登录**：
   ```bash
   ./setup_ssh_免密.sh
   ```
   - 若在yum源配置时已完成免密配置，则无需重复执行

4. **执行批量基线加固**：
   ```bash
   ./batch_deploy.sh
   ```
   - 支持多线程执行：`./batch_deploy.sh -t n`（n为整数）
   - 可指定主节点服务器：`./batch_deploy.sh -m root@IP`，默认为当前服务器

5. **测试脚本（可选）**：
   ```bash
   ./kylin_baseline.sh
   ```
   - 通过传入`--with-xlsx`参数可生成XLSX格式的结果报告
   - 当配置`CHECK_SYSTEM_UPDATES=true`时会启用系统更新，同时会更新最新内核

### 3. 查看结果

- 加固结果文件将保存到`互联网结果/`或`政务外网结果/`目录中
- 每个服务器对应两个结果文件：
  - Markdown格式：`IP_baseline_日期.md`
  - CSV格式：`IP_baseline_result.csv`
- 可使用结果目录中的美化脚本进一步格式化报告

## 逻辑关联说明

### 系统工作流程图

```
┌─────────────────┐     ┌────────────────────────┐     ┌───────────────────┐
│  资产表文件     │────▶│generate_server_list.sh │────▶│   servers.txt     │
│  (CSV/XLSX)     │     │  （服务器列表生成）     │     │.server_passwords.txt│
└─────────────────┘     └────────────────────────┘     └─────────┬─────────┘
                                                                 │
                                                                 ▼
                         ┌─────────────────────────────────────┐
                         │            配置流程选择             │
                         └───────────────┬─────────────────────┘
                                         │
           ┌─────────────────────────────┴─────────────────────────────┐
           │                                                           │
           ▼                                                           ▼
┌─────────────────────────────────────┐     ┌─────────────────────────────────────┐
│          yum源配置流程              │     │          基线加固配置流程           │
└─────────────────────────────────────┘     └─────────────────────────────────────┘
           │                                                           │
           ▼                                                           ▼
┌─────────────────────────────────────────────────┐     ┌─────────────────────────────────────┐
│  /home/yumConfig/目录下：                       │     │  /home/baseline/目录下：            │
│  - config_kylin_yum.sh                          │     │  - batch_deploy.sh                  │
│  - setup_ssh_免密.sh                            │     │  - generate_server_list.sh          │
│  - generate_server_list.sh                      │     │  - kylin_baseline.sh                │
└───────────┬─────────────────────────────────────┘     └───────────┬─────────────────────────┘
            │                                                        │
            ▼                                                        ▼
┌─────────────────────────────────────────────────┐     ┌─────────────────────────────────────┐
│ setup_ssh_免密.sh 或 config_kylin_yum.sh        │     │ 1. generate_server_list.sh生成      │
│ 配置SSH免密（config_kylin_yum.sh会自动调用）    │     │    servers.txt和密码文件            │
└───────────┬─────────────────────────────────────┘     └───────────┬─────────────────────────┘
            │                                                        │
            ▼                                                        ▼
┌─────────────────────────────────────────────────┐     ┌─────────────────────────────────────┐
│ config_kylin_yum.sh 传入源IP（.yumIP.env中查找）│     │ 2. setup_ssh_免密.sh配置SSH免密登录 │
│ 根据IP前缀识别网络环境，配置对应yum源           │     │    （若yum源已配置则无需重复执行）  │
└─────────────────────────────────────────────────┘     └───────────┬─────────────────────────┘
                                                                   │
                                                                   ▼
                                                         ┌─────────────────────────────────────┐
                                                         │ 3. batch_deploy.sh批量部署基线加固  │
                                                         │    - 支持多线程：-t n              │
                                                         │    - 支持指定主节点：-m root@IP     │
                                                         └───────────┬─────────────────────────┘
                                                                   │
                                                                   ▼
                                                         ┌─────────────────────────────────────┐
                                                         │ 4. 远程服务器执行kylin_baseline.sh  │
                                                         │    - 生成CSV和Markdown格式结果      │
                                                         │    - 支持--with-xlsx参数生成XLSX报告│
                                                         │    - 可选配置CHECK_SYSTEM_UPDATES=true│
                                                         └───────────┬─────────────────────────┘
                                                                   │
                                                                   ▼
                                                     ┌───────────────────────────────────────┐
                                                     │ 下载结果到本地结果目录                │
                                                     │ - 互联网结果/                         │
                                                     │ - 政务外网结果/                       │
                                                     └───────────────────────────────────────┘
```

### 脚本工作流程

1. **数据准备流**：
   - `generate_server_list.sh` → 从资产表生成服务器列表和密码文件 → 提供给`setup_ssh_免密.sh`和`batch_deploy.sh`使用
   - 资产表支持CSV/XLSX格式，自动生成`servers.txt`和`.server_passwords.txt`

2. **SSH免密配置流**：
   - `setup_ssh_免密.sh` → 生成专用SSH密钥对 → 配置到所有目标服务器
   - 可单独执行，也可由`config_kylin_yum.sh`自动调用
   - 生成专用密钥文件：`~/.ssh/id_rsa_ssh_setup`

3. **Yum源配置流**：
   - `config_kylin_yum.sh` → 检查并执行SSH免密配置（如需要）
   - 从`.yumIP.env`获取源IP，根据IP前缀识别网络环境（互联网/政务外网）
   - 根据系统版本自动选择合适的yum源配置

4. **基线加固流**：
   - `batch_deploy.sh` → 读取服务器列表 → 部署`kylin_baseline.sh`到远程服务器
   - 支持多线程并行执行（-t参数）和指定主节点（-m参数）
   - `kylin_baseline.sh` → 在远程服务器执行基线检查和加固 → 生成结果文件
   - 支持生成XLSX格式报告（--with-xlsx参数）和系统更新（CHECK_SYSTEM_UPDATES配置）
   - 结果文件自动下载到对应网络环境的结果目录中

### 核心依赖关系

- `batch_deploy.sh` 依赖 `kylin_baseline.sh` 作为实际执行加固的脚本
- `config_kylin_yum.sh` 依赖 `setup_ssh_免密.sh` 进行SSH免密配置
- 多个脚本依赖服务器列表和密码文件作为输入
- 结果文件格式为CSV和Markdown，便于后续处理和查看

## 注意事项

- 执行基线加固前请确保已备份重要数据
- 某些加固操作可能需要重启服务或系统
- 请根据实际环境调整脚本中的参数设置
- 确保有足够的权限执行所有操作（大部分脚本需要root权限）
- 如需使用XLSX格式转换功能，请确保已安装python3和必要的依赖包

## 维护信息

本项目适用于麒麟操作系统的安全基线加固和yum配置管理。如有任何问题或建议，请联系项目维护者。
## Last
***markdown、csv结果报告美化太恶心辣（肝了一宿，焯{建议不要用Qoder这玩意删代码👊}）***